# Copy to .env and replace values.
JWT_SECRET=generate-a-long-random-string-for-example-use-openssl-rand-hex-32

PORT=5001
MONGO_URI=mongodb://localhost:27017/shuttle_tracking

# First-time empty DB: use the SAME URI in Compass, then run:  npm run seed:dev
